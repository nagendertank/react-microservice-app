import LoadBundles from '../src/LoadBundlesUtils';

const getInstance = () => {
  return wrapper.instance();
};

var wrapper = null;


describe('Test Load Bundles Utils.', () => {
  test('Load JS Method', () => {
    
   
  });
  
});
